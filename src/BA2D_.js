
class BA2D_
{
    /**
     * Loads a BA2D file into memory.
     */
    static load(
        file, 
        dims,
        { 
            onProgress = null, 
            type = "float" 
        } = {}
    ) {

    }

    /** 
     * Saves a BA2D file into memory 
     */
    static save(
        file, 
        dims, 
        array,
        { 
            onProgress = null, 
            type = "float" 
        } = {}
    ) {

    }
}